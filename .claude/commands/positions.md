# Positions

Call `broker_positions` and `broker_account_summary` in parallel.

Show:
- Total portfolio value and cash balance
- Position table: `TICKER  shares  avg_cost  market_value  P&L%` (sort by market_value desc)

One line per position. No disclaimers.
