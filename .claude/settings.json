{
  "permissions": {
    "allow": [
      "Bash(ls:*)",
      "Bash(ls -la:*)",
      "Bash(find:*)",
      "Bash(grep:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(cat:*)",
      "Bash(make:*)",
      "Read(*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); case \"$FILE\" in *.py) uv run ruff format \"$FILE\" >/dev/null 2>&1 || true;; esac"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); case \"$FILE\" in *.env.example|*.env.sample) ;; *uv.lock|*.env|*.env.*|*maverick_mcp.db*|*alembic/versions/*.py) echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Protected path — lockfile/env/db/applied-migration edits require explicit human action, not agent Edit/Write.\"}}';; esac"
          }
        ]
      }
    ]
  }
}
